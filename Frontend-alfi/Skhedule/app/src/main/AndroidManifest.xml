<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alfred.karela.schedule">
     
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PrivateEvent"></activity>
        <activity android:name=".PublicEvent"></activity>
        <activity android:name=".PolledEvent"></activity>
        <activity android:name=".Remainders"></activity>
        <activity android:name=".Datepicker"></activity>
        <activity android:name=".EditPro"></activity>
        <activity android:name=".LoadingActivity"></activity>
        <activity android:name=".EventView"></activity>
        <activity android:name=".LoginActivity" android:label="User Login">

        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="User Login"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" android:label="User Sigup" />
    </application>

</manifest>
